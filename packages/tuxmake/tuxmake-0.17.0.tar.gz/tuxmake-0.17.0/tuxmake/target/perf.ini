[target]
description = "perf programs and library"
commands = {make} -C tools/perf
    && {make} -C tools/perf install-bin DESTDIR={build_dir}/perf-install/usr
    && tar -caf {build_dir}/perf.tar.xz -C {build_dir}/perf-install .

[artifacts]
perf.tar.xz = perf.tar.xz
