[target]
description = "Kernel headers"
dependencies = config
commands = {make} headers_install INSTALL_HDR_PATH={build_dir}/install_hdr/
    && tar caf {build_dir}/headers.tar.xz -C {build_dir}/install_hdr .

[artifacts]
headers.tar.xz = headers.tar.xz
