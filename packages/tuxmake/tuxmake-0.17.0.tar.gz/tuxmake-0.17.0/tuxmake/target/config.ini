[target]
description = "Kernel configuration"
# There is no "commands" here because generating the .config file depends on
# command line arguments, target architecture, etc. See Config::prepare().

[artifacts]
config = .config
