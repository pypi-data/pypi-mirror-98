# import setuptools
# from distutils.core import setup
from setuptools import setup
setup(
      name = 'enisebook',
      packages = ['enisebook'],
      version = '0.0.1.3',
      description = 'A lib for making interactive books',
      author = 'Greg',
      author_email = 'gregory.debono@enise.fr',
      keywords = ['interactive', 'book'], # arbitrary keywords
      install_requires=[
      ],
      classifiers = [],
)
