from setuptools import setup

setup(name = 'flip_dsnd_probability',
      version = '0.1',
      description = 'Gaussian and Binomial distributions',
      packages = ['flip_dsnd_probability'],
      author = 'Felipe Gonçalves',
      zip_safe = False)
