<button tal:define="value request.localizer.translate(view.value);
					hint view.hint | nothing;"
		type="button"
		id="${view.id}"
		name="${view.name}"
		class="btn btn-secondary ${view.klass} ${'hint' if hint else ''}"
		style="${view.style}"
		lang="${view.lang}"
		value="${view.value}"
		disabled="${view.disabled}"
		tabindex="${view.tabindex}"
		readonly="${view.readonly}"
		alt="${view.alt}"
		accesskey="${view.accesskey}"
		onclick="${view.onclick}"
		ondblclick="${view.ondblclick}"
		onmousedown="${view.onmousedown}"
		onmouseup="${view.onmouseup}"
		onmouseover="${view.onmouseover}"
		onmousemove="${view.onmousemove}"
		onmouseout="${view.onmouseout}"
		onkeypress="${view.onkeypress}"
		onkeydown="${view.onkeydown}"
		onkeyup="${view.onkeyup}"
		onfocus="${view.onfocus}"
		onblur="${view.onblur}"
		onchange="${view.onchange}"
		onselect="${view.onselect}"
		data-loading-test="${value}..."
		data-ams-url="${view.href | nothing}"
		data-toggle="${'modal' if getattr(view, 'modal_target', False) == True else nothing}"
		data-original-title="${hint}"
		data-ams-data='${tales:object_data(view)}'>${value}</button>