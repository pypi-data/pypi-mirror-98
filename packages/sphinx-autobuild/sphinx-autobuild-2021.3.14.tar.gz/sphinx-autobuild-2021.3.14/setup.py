#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['sphinx_autobuild']

package_data = \
{'': ['*']}

package_dir = \
{'': 'src'}

install_requires = \
['sphinx', 'livereload', 'colorama']

extras_require = \
{'test': ['pytest', 'pytest-cov']}

entry_points = \
{'console_scripts': ['sphinx-autobuild = sphinx_autobuild.__main__:main']}

setup(name='sphinx-autobuild',
      version='2021.3.14',
      description='Rebuild Sphinx documentation on changes, with live-reload in the browser.',
      author='Jonathan Stoppani',
      author_email='jonathan@stoppani.name',
      url='https://github.com/executablebooks/sphinx-autobuild',
      packages=packages,
      package_data=package_data,
      package_dir=package_dir,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
