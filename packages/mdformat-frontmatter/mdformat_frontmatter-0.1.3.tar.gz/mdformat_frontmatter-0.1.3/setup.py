#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mdformat_frontmatter']

package_data = \
{'': ['*']}

install_requires = \
['mdformat >=0.5.3,<0.6.0', 'mdit-py-plugins', 'pyyaml']

extras_require = \
{'dev': ['pre-commit'], 'test': ['pytest~=6.0', 'coverage', 'pytest-cov']}

entry_points = \
{'mdformat.parser_extension': ['frontmatter = mdformat_frontmatter']}

setup(name='mdformat_frontmatter',
      version='0.1.3',
      description='An mdformat plugin for parsing / ignoring frontmatter.',
      author='Chris Butler',
      author_email='chris@thebutlers.me',
      url='https://github.com/butler54/mdformat-frontmatter',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
