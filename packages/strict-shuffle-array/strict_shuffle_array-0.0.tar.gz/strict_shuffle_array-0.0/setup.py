from setuptools import setup

setup(name='strict_shuffle_array',
      version='0.0',
      author='Cringe_code',
      description='Shuffles the array so that any element takes a new place',
      packages=['strict_shuffle_array'],
      author_email='bibikonfikus1234@gmail.com',
      zip_safe=False)
