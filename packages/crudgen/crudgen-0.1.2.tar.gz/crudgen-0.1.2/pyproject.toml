[tool.poetry]
name = "crudgen"
version = "0.1.2"
description = "a minimal CRUD code generator"
authors = ["pouria <pooriazmn@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
