from setuptools import setup

setup(name='nlp_model_flow',
      version='0.1',
      description='workCloud analysis and linear model activation',
      packages=['nlp_model_flow'],
      zip_safe=False)
